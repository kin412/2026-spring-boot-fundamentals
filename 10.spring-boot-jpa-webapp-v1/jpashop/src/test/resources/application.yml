#test 하위의 application.yml은 test에서만 적용되는데
#아래내용이 없어도 부트가 자동으로 인메모리로 작동한다.

#spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop //테스트는 기본적으로 create-drop
#    properties:
#      hibernate:
#       show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace #스프링 부트 2.x, hibernate5
  org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6